

Edit and Run batch file

Executable requires 4 arguments:

population size as integer
max generations as integer
pc (probability of crossover) as decimal between 1 and 0
pm (probability of mutation) as decimal between 1 and 0

example batch file:

start "" "Source.exe" 50 10000 .50 .01
